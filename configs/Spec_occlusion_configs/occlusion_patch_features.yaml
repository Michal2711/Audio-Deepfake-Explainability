dataset:
  # dictionary with results of occlusion explainability experiment
  occlusion_result_path: "results/SpectrogramExplainability/FakeRealMusicOriginal/Occlusion/STFT_Occlusion_1024_20pct_stride_10pct"

output:
  # dictionary where to save extracted features from occlusion patches
  result_path: "results/SpectrogramExplainability/FakeRealMusicOriginal/Occlusion/STFT_Occlusion_1024_20pct_stride_10pct/occlusion_features"
  experiment_name: "occlusion_patches"

audio:
  samplerate: 44100

occlusion_patches:
  groups: ["best", "worst", "most_influential"]  # available: all, best, worst, most_influential
